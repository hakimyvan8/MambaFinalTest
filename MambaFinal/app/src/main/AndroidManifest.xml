<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mambafinal">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:replace="android:theme"
        android:theme="@style/AppTheme">
        <activity android:name=".Client.LoginSupplierActivity"></activity>
        <activity android:name=".Client.HomeSupplierActivity" />
        <activity android:name=".Client.OrderActivity" />
        <activity android:name=".Client.ResetPasswordActivity" />
        <activity android:name=".Client.ConfirmFinalorderActivity" />
        <activity android:name=".Client.CartActivity" />
        <activity android:name=".Client.ProductdetailsActivity" />
        <activity android:name=".Client.SettingsActivity" />
        <activity
            android:name=".Client.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Client.RegisterActivity" />
        <activity android:name=".Client.LoginActivity" />
        <activity android:name=".Client.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>